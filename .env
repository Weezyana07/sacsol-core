# .env

# Django
DJANGO_SETTINGS_MODULE=sacsol.settings
DEBUG=true
SECRET_KEY=django-insecure-gbnf=#+)bd7feapd=6!@8y##0m&a^eno+rbb&ky5u3r_7b^f#o
ALLOWED_HOSTS=localhost,127.0.0.1


# Database (Postgres or fallback to sqlite when empty)
# DATABASE_URL=postgres://inventory:inventory@localhost:5432/inventory
# Database (set these to use Postgres; leave DB_HOST empty to fall back to SQLite)
DB_NAME=inventory
DB_USER=postgres
DB_PASSWORD=sacsol
DB_HOST=localhost
DB_PORT=5432

# Email (fill if you’ll email LPO PDFs)
EMAIL_HOST=
EMAIL_PORT=587
EMAIL_HOST_USER=
EMAIL_HOST_PASSWORD=
EMAIL_USE_TLS=true
DEFAULT_FROM_EMAIL=no-reply@example.com

# Features
FEATURE_EMAIL_CUSTOMER_ON_SEND=false
FEATURE_EMAIL_CUSTOMER_ON_MANUAL_PAID=true

# Time
TIMEZONE=Africa/Lagos

# CORS/CSRF (add your frontend origins with scheme)
CORS_ALLOWED_ORIGINS=http://localhost:3000
CSRF_TRUSTED_ORIGINS=http://localhost:3000

# Procurement
MAX_PDF_UPLOAD_MB=5
IMAGE_MAX_DIM=2000
MAX_IMAGE_UPLOAD_KB=300
# (Optional) Public URLs for PDF links/verify pages
SITE_URL=http://localhost:8000
FRONTEND_BASE_URL=http://localhost:3000
